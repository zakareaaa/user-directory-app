[project]
name = "backend"
version = "0.1.0"
description = "FastAPI backend for the user directory application"
readme = "README.md"
authors = [
    { name = "zakareaaa", email = "zakrea.alkashef@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "psycopg[binary]>=3.3.4",
    "sqlalchemy>=2.0.52",
    "uvicorn[standard]>=0.52.3",
]

[tool.uv]
package = false